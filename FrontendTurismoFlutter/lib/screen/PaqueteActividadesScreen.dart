import 'package:flutter/material.dart';

class PaqueteActividadesScreen extends StatelessWidget {
  const PaqueteActividadesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Pantalla de Paquete de actividades',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
