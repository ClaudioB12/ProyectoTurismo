import 'package:flutter/material.dart';

class PaqueteDestinoScreen extends StatelessWidget {
  const PaqueteDestinoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Pantalla de Paquete Destino',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
